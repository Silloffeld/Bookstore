@model IEnumerable<BookStore.Models.Category>
@{
    ViewData["Title"] = "Categorieën";
}
<div class="d-flex justify-content-between mb-3">
    <h2>Categorieën</h2>
    <a class="btn btn-success" asp-action="Create">Nieuwe categorie</a>
</div>
<table class="table table-striped">
    <thead>
        <tr><th>Id</th><th>Naam</th><th>Volgorde</th><th>Actions</th></tr>
    </thead>
    <tbody>
    @foreach(var c in Model)
    {
        <tr>
            <td>@c.Id</td>
            <td>@c.Name</td>
            <td>@c.DisplayOrder</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@c.Id">Edit</a>
                <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@c.Id">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>
